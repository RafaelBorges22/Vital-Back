services:
  - type: web
    name: vitalreciclagem-api
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn main:app"
    preDeployCommand: "./migrate.sh" 
    plan: free
    numInstances: 1
